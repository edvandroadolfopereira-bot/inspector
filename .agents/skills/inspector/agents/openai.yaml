interface:
  display_name: "Inspector"
  short_description: "Repo-specific patterns and workflows for inspector"
  default_prompt: "Use the inspector repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true